using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShaderAlphaController : MonoBehaviour {
    /*
    private void OnCollisionEnter(Collision collision)
    {
        Debug.Log("Collide with: " + collision.transform.name);
        Debug.Log(collision.transform.position);
        // stransform.GetComponent<Renderer>().material.SetColor("_TintColor", Color.red);
    }
    
    private void OnCollisionStay(Collision collision)
    {
        foreach (ContactPoint contact in collision.contacts)
        {
            // print(contact.thisCollider.name + " hit " + contact.otherCollider.name);
            // Debug.Log(contact.otherCollider.transform.position);
            // Debug.DrawRay(contact.point, contact.normal, Color.white);
        }
    }*/
}
